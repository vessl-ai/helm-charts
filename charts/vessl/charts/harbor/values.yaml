harbor:
  clusterIP:
  databasePassword:
  harborAdminPassword:
  mirrorSourceType: quay
  nodeSelector:
    kubernetes.io/hostname: master
  registryPassword:
  tolerations:
    - effect: NoSchedule
      key: node-role.kubernetes.io/master
      operator: Exists
